# Native Windows / Linux GPU fine-tuning stack for train_sancta_llm.py
# Install PyTorch with CUDA first, then:
#   pip install -r requirements-training.txt

transformers>=4.46.0
accelerate>=0.34.0
peft>=0.12.0
trl>=0.29.0
datasets>=3.0.0
bitsandbytes>=0.43.0
safetensors>=0.4.0

# PyTorch (pick ONE line for your platform — do not duplicate):
# CUDA 12.4 wheels (Windows/Linux):
#   pip install torch --index-url https://download.pytorch.org/whl/cu124
